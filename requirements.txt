# Web & API
python-dotenv==1.0.1
fastapi==0.129.2
apscheduler>=3.10.0
uvicorn==0.41.0
pydantic==2.12.5
websockets==16.0

# System & async
psutil==7.2.2
numpy>=1.26,<2.3

# Audio I/O (STT capture)
# System dependency (Debian/Ubuntu): sudo apt-get update && sudo apt-get install -y portaudio19-dev
PyAudio==0.2.14

# Speech-to-text
faster-whisper==1.2.1
vosk==0.3.45
scipy>=1.11,<1.15

# Text-to-speech (Piper)
piper-tts==1.4.1
onnxruntime>=1.17,<1.21

# Local LLM & model download
llama-cpp-python==0.3.16
huggingface-hub==1.4.1

# Web search (tool_ai)
ddgs>=9.0.0

# Optional: Raspberry Pi camera (install on device; often via apt or --system-site-packages)
# picamera2==0.3.34

# Semantic routing (semantic_router.py): pip install "semantic-router[local]"
# semantic-router[local]

# Hailo object detection (object_detection.py + hailo_od)
# lap>=0.4.0
# cython_bbox  # may need building on Pi; install from project .venv
PyYAML>=6.0

# Testing (optional): pip install pytest then run pytest tests/
pytest>=8.0
