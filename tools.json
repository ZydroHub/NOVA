[
  {
    "name": "get_weather",
    "description": "Retrieves current weather or forecast for a specific location.",
    "parameters": {
      "type": "object",
      "properties": {
        "location": {
          "type": "string",
          "description": "The city and state/country, e.g., 'San Francisco, CA' or 'local'"
        }
      },
      "required": [
        "location"
      ]
    }
  },
  {
    "name": "web_search",
    "description": "Performs a live internet search to retrieve snippets for current events or facts.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query"
        }
      },
      "required": [
        "query"
      ]
    }
  },
  {
    "name": "network_scan",
    "description": "Scans the local network (LAN) to identify connected devices and open ports.",
    "parameters": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "get_stock_price",
    "description": "Retrieves the current stock price and basic market data for a given ticker symbol.",
    "parameters": {
      "type": "object",
      "properties": {
        "symbol": {
          "type": "string",
          "description": "The stock ticker symbol, e.g., 'AAPL', 'GOOGL', 'TSLA'"
        }
      },
      "required": [
        "symbol"
      ]
    }
  }
]